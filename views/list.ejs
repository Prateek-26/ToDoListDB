<%- include('header'); -%>
<body>
    <div class="box" id="heading">
        <h1> <%= listTitle %> </h1>
    </div>

    <div class="box">

            <!-- <% for(var i=0;i<newItemsAdded.length;i++) { %>

                <div class="item">
                    <input type="checkbox">
                    <p> <%= newItemsAdded[i].name %> </p>
                </div>
               <% } %> -->
            <form action="/delete" method="post">
                <% newItemsAdded.forEach(function(item){ %>
                <div class="item">
                    <input type="checkbox" onchange="this.form.submit()" name="checkbox" value="<%= item._id %>">
                    <p> <%= item.name %> </p>
                </div>
                <% })  %>
                <input type="hidden" name="hiddenListName" value="<%= listTitle %>"></input>
            </form>
            
               <form class="item" action="/" method="post">
                <input type="text" name="newItem" placeholder="New Item to be addded" autocomplete="off">
                <button type="submit" name="addButton" value=<%=listTitle%>>+</button>
            </form>

    </div>
</body>

<%- include('footer'); -%>
